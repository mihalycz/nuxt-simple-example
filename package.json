{
  "name": "front",
  "version": "1.0.0",
  "description": "nuxt.js simple example",
  "author": "mihalycz",
  "scripts": {
    "postinstall": "cd front && npm i && export $(cat .env | xargs)",
    "start": "nuxt generate && node ./node_modules/.bin/http-server ./dist"
  },
  "engines": {
    "node": "10.x"
  }
}